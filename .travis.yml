# Travis example for Identifier created by Rafael Garibotti

sudo: false
language: c
compiler:
  - gcc
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc
      - valgrind
      - cppcheck
install:
  - "[ $GCC = gcc ] && export GCC=gcc || true"
script:
  - echo "abc" > input.txt
  - echo "a" >> input.txt
  - echo "b" >> input.txt
  - echo "c" >> input.txt
  - make cppcheck
  - make ARGS=input.txt valgrind
  - cat out.txt
  - make ARGS=input.txt sanitizers
  - cat out.txt
  #- make test
